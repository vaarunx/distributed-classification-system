# Load Testing Configuration
# Copy this file to .env and update with your values
# cp .env.example .env

# Backend API URL (ALB endpoint or localhost for testing)
# Get this from: terraform output alb_endpoint
BACKEND_API_URL=http://localhost:8080

# Image folder path (relative to project root)
IMAGE_FOLDER=images

# Number of images to upload (default: 1000, max: 2000)
IMAGE_COUNT=1000

# S3 keys mapping file
S3_KEYS_FILE=s3_keys.json

# AWS Configuration
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# Note: If not set, boto3 will use default credentials (IAM role, ~/.aws/credentials, etc.)

# Output directories
GRAPH_OUTPUT_DIR=reports
RESULTS_DIR=results
